# Basic Informational Site

Simple site served from the server side in NodeJS for The Odin Project.

We have a few static html pages and some basic server routing.
If you'd like to run this locally just clone the repo and run `node index.js`. Then open <http://localhost:8080/>.
